@model CarnetDigital.Frontend.Models.Shared.PagedResult<CarnetDigital.Frontend.Models.Areas.AreaDto>
@{
    ViewData["Title"] = "Áreas de trabajo";
}

<h1>Áreas de trabajo</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo</a>
</p>

@if (!Model.Items.Any())
{
    <p>No hay áreas de trabajo registradas.</p>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var area in Model.Items)
            {
                <tr>
                    <td>@area.Id</td>
                    <td>@area.Nombre</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@area.Id" class="btn btn-sm btn-outline-secondary">Editar</a>
                        <button type="button" class="btn btn-sm btn-outline-danger"
                                data-bs-toggle="modal" data-bs-target="#deleteModal"
                                data-delete-url="@Url.Action("Delete", new { id = area.Id })"
                                data-nombre="@area.Nombre">
                            Eliminar
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @if (Model.TotalPages > 1)
    {
        <nav aria-label="Paginación de áreas de trabajo">
            <ul class="pagination">
                <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                    <a class="page-link" asp-action="Index" asp-route-page="@(Model.PageNumber - 1)">Anterior</a>
                </li>
                <li class="page-item disabled">
                    <span class="page-link">Página @Model.PageNumber de @Model.TotalPages</span>
                </li>
                <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                    <a class="page-link" asp-action="Index" asp-route-page="@(Model.PageNumber + 1)">Siguiente</a>
                </li>
            </ul>
        </nav>
    }
}

<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalTitle" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form id="deleteForm" asp-action="Delete" method="post">
                <div class="modal-header">
                    <h5 class="modal-title" id="deleteModalTitle">Confirmar eliminación</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>¿Realmente desea eliminar el elemento seleccionado?</p>
                    <p class="fw-bold" id="deleteModalNombre"></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">No</button>
                    <button type="submit" class="btn btn-danger">Sí</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/confirm-delete.js"></script>
}
